import React from 'react'
import ReactDOM from 'react-dom/client'
import { FormLoginApp } from './form-login-app';


ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <FormLoginApp />
  </React.StrictMode>
);
